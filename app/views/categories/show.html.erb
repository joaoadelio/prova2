<p id="notice"><%= notice %></p>

<p>
  <h3><strong>Descricao:</strong></h3>
  <hr>
  <h6><%= @category.descricao %></h6>
</p>

<%= link_to 'Editar', edit_category_path(@category), :class=> "btn btn-primary" %> |
<%= link_to 'Voltar', categories_path, :class=> "btn btn-info" %>
